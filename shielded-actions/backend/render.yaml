services:
  - type: web
    name: shielded-actions-backend
    runtime: docker
    region: oregon
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: MIX_ENV
        value: prod
